#ifndef INPUT_FILE_PROCESS
#define INPUT_FILE_PROCESS

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "Linkedlistprocess.h"

Process *loadInputFile(char *filename, int *numProcesses);
#endif // PROCESS_LIST
